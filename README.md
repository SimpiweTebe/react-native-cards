# Card Item compoents

UI design for card components to be potentially used on game UI store.

### Install instructions

- Run npm install to install
- Run npx pod-install if working on Mac OSX
- npx react-native run-android (android)
- npx react-native run-ios (iOS)

<img src="images/list.png" alt="Project Screenshot" width="439" height="902">
